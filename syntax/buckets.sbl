// Buckets are a lot like specifying specific allocators to use, but they behave more like Arenas.

use alloca;

astBucket := Bucket(alloca.gpa);
astTree := new AstTree with astBucket; // typeof astTree = `*AstTree`

