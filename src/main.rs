mod ast;
mod common;
mod parse;
mod scan;

fn main() {
    // let input = "mut x = true;".to_string();
    // let mut parser = parse::Parser::new(&input);
    // let parse_tree = parser.parse();
    // print_parse_tree(std::io::stderr(), &input, &parse_tree, Handle::new(1));
}
